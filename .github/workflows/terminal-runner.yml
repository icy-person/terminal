name: GitHub Hosted Terminal Runner (Arch)

on:
  workflow_dispatch:
    inputs:
      shell:
        description: "Shell to run inside the hosted runner"
        required: true
        default: "bash"
        type: choice
        options: [bash, sh]
      idle_seconds:
        description: "Health-check interval"
        required: true
        default: "5"
        type: string

permissions:
  contents: read

concurrency:
  group: hosted-terminal-fixed
  cancel-in-progress: true

jobs:
  terminal:
    name: Hosted terminal + noVNC desktop (Arch)
    runs-on: ubuntu-26.04
    timeout-minutes: 350

    container:
      image: archlinux:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node dependencies
        shell: bash
        run: |
          set -euo pipefail
          pacman -Syu --noconfirm
          pacman -S --needed --noconfirm nodejs npm python python-pip curl ca-certificates
          npm install --no-audit --no-fund --ignore-scripts

      - name: Install terminal and desktop stack
        shell: bash
        run: |
          set -euo pipefail

          pacman -S --needed --noconfirm \
            xfce4 \
            xfce4-terminal \
            dbus \
            xorg-server-xvfb \
            x11vnc \
            xorg-xdpyinfo \
            curl

          NOVNC_VERSION="1.7.0"
          WEBSOCKIFY_VERSION="0.13.0"
          NOVNC_DIR="$RUNNER_TEMP/noVNC"
          WEBSOCKIFY_DIR="$RUNNER_TEMP/websockify"

          rm -rf "$NOVNC_DIR" "$WEBSOCKIFY_DIR"
          mkdir -p "$NOVNC_DIR" "$WEBSOCKIFY_DIR"

          curl -fsSL "https://github.com/novnc/noVNC/archive/refs/tags/v${NOVNC_VERSION}.tar.gz" \
            | tar -xz --strip-components=1 -C "$NOVNC_DIR"

          curl -fsSL "https://github.com/novnc/websockify/archive/refs/tags/v${WEBSOCKIFY_VERSION}.tar.gz" \
            | tar -xz --strip-components=1 -C "$WEBSOCKIFY_DIR"

          python3 -m pip install --break-system-packages --no-cache-dir --no-deps "$WEBSOCKIFY_DIR"

          echo "NOVNC_DIR=$NOVNC_DIR" >> "$GITHUB_ENV"

          curl -fsSL "https://github.com/tsl0922/ttyd/releases/download/1.7.7/ttyd.x86_64" \
            -o "$RUNNER_TEMP/ttyd"
          chmod +x "$RUNNER_TEMP/ttyd"
          "$RUNNER_TEMP/ttyd" --version

          curl -fsSL "https://github.com/cloudflare/cloudflared/releases/download/2026.9.1/cloudflared-linux-amd64" \
            -o "$RUNNER_TEMP/cloudflared"
          chmod +x "$RUNNER_TEMP/cloudflared"
          "$RUNNER_TEMP/cloudflared" --version

          Xvfb -help >/dev/null 2>&1 || true
          x11vnc -version 2>&1 | head -n 1 || true
          python3 -m websockify --help >/dev/null 2>&1
          command -v websockify >/dev/null
          echo "websockify: $(command -v websockify)"
          test -f "$NOVNC_DIR/vnc.html"

      - name: Show Arch environment
        shell: bash
        run: |
          set -euo pipefail
          echo "=== OS ==="
          cat /etc/os-release
          echo
          echo "=== Kernel ==="
          uname -a
          echo
          echo "=== glibc ==="
          ldd --version | head -n 1
          echo
          echo "=== CPU ==="
          nproc
          awk '/^model name/ {print; exit}' /proc/cpuinfo

      - name: Start virtual desktop
        env:
          DISPLAY: :99
        shell: bash
        run: |
          set -euo pipefail
          echo 'DISPLAY=:99' >> "$GITHUB_ENV"

          nohup Xvfb :99 -screen 0 1920x1080x24 -ac +extension GLX +render -noreset \
            >"$RUNNER_TEMP/xvfb.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/xvfb.pid"

          for _ in $(seq 1 30); do
            xdpyinfo -display :99 >/dev/null 2>&1 && break
            sleep 1
          done
          xdpyinfo -display :99 >/dev/null

          nohup dbus-run-session -- startxfce4 \
            >"$RUNNER_TEMP/xfce.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/xfce.pid"
          sleep 3

          nohup x11vnc -display :99 -forever -shared -localhost -rfbport 5900 -noxdamage -nopw \
            >"$RUNNER_TEMP/x11vnc.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/x11vnc.pid"

          nohup websockify --web="$NOVNC_DIR" 6080 127.0.0.1:5900 \
            >"$RUNNER_TEMP/websockify.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/websockify.pid"

          for _ in $(seq 1 30); do
            curl -fsS http://127.0.0.1:6080/vnc.html >/dev/null 2>&1 && break
            sleep 1
          done
          curl -fsS http://127.0.0.1:6080/vnc.html >/dev/null
          kill -0 "$(cat "$RUNNER_TEMP/xvfb.pid")"
          kill -0 "$(cat "$RUNNER_TEMP/x11vnc.pid")"
          kill -0 "$(cat "$RUNNER_TEMP/websockify.pid")"
          echo "DESKTOP_LOCAL=http://127.0.0.1:6080/vnc.html"

      - name: Start hosted PTY and relay
        env:
          TERMINAL_SHELL: ${{ inputs.shell || 'bash' }}
        shell: bash
        run: |
          set -euo pipefail
          TOKEN="$(python3 -c 'import secrets; print(secrets.token_hex(32))')"
          echo "TERMINAL_TOKEN=$TOKEN" >> "$GITHUB_ENV"
          export TERMINAL_TOKEN="$TOKEN"

          if [ "$TERMINAL_SHELL" = "sh" ]; then
            SHELL_CMD=/bin/sh
            SHELL_ARGS=()
          else
            SHELL_CMD=/bin/bash
            SHELL_ARGS=(--login)
          fi

          nohup "$RUNNER_TEMP/ttyd" -W -p 7681 -i 127.0.0.1 -P 8 "$SHELL_CMD" "${SHELL_ARGS[@]}" \
            >"$RUNNER_TEMP/ttyd.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/ttyd.pid"

          export NOVNC_HTTP_URL="http://127.0.0.1:6080"
          export NOVNC_WS_URL="ws://127.0.0.1:6080/websockify"
          nohup npm run relay >"$RUNNER_TEMP/relay.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/relay.pid"

          for _ in $(seq 1 30); do
            curl -fsS http://127.0.0.1:8090/healthz >/dev/null 2>&1 && break
            sleep 1
          done
          curl -fsS http://127.0.0.1:8090/healthz >/dev/null
          curl -fsS http://127.0.0.1:8090/novnc/vnc.html >/dev/null
          kill -0 "$(cat "$RUNNER_TEMP/ttyd.pid")"
          kill -0 "$(cat "$RUNNER_TEMP/relay.pid")"

      - name: Start public Quick Tunnel
        shell: bash
        run: |
          set -euo pipefail
          nohup "$RUNNER_TEMP/cloudflared" tunnel --no-autoupdate --url http://127.0.0.1:8090 \
            >"$RUNNER_TEMP/cloudflared.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/cloudflared.pid"

          URL=""
          for _ in $(seq 1 60); do
            URL="$(grep -Eo 'https://[-a-zA-Z0-9]+\.trycloudflare\.com' "$RUNNER_TEMP/cloudflared.log" | head -n1 || true)"
            [ -n "$URL" ] && break
            sleep 2
          done
          [ -n "$URL" ] || { cat "$RUNNER_TEMP/cloudflared.log"; exit 1; }

          TOKEN_QS="$(python3 -c 'import urllib.parse,sys; print(urllib.parse.quote(sys.argv[1], safe=""))' "$TERMINAL_TOKEN")"
          WS_URL="${URL/https:\/\//wss://}/ws"
          ENDPOINT_URL="https://icy-person.github.io/terminal/?endpoint=$(python3 -c 'import urllib.parse,sys; print(urllib.parse.quote(sys.argv[1], safe=""))' "$WS_URL")&token=$TOKEN_QS"

          NOVNC_URL="$URL/novnc/vnc.html"

          echo "TERMINAL_URL=$URL" >> "$GITHUB_ENV"
          echo "TERMINAL_WS_URL=$WS_URL" >> "$GITHUB_ENV"
          echo "TERMINAL_PAGES_URL=$ENDPOINT_URL" >> "$GITHUB_ENV"
          echo "NOVNC_URL=$NOVNC_URL" >> "$GITHUB_ENV"
          echo "HOSTED TERMINAL: $ENDPOINT_URL"
          echo "HOSTED DESKTOP: $NOVNC_URL"

      - name: End-to-end diagnostics
        shell: bash
        run: |
          set -euo pipefail
          echo "[1/4] local relay health"
          curl -fsS http://127.0.0.1:8090/healthz | python3 -m json.tool

          echo "[2/4] local noVNC"
          curl -fsSI http://127.0.0.1:8090/novnc/vnc.html

          echo "[3/4] public health"
          PUBLIC_OK=0
          PUBLIC_HOST="${TERMINAL_URL#https://}"
          for _ in $(seq 1 30); do
            if getent ahostsv4 "$PUBLIC_HOST" >/dev/null 2>&1 && \
               curl -4 --connect-timeout 5 --max-time 15 -fsS "$TERMINAL_URL/healthz" | python3 -m json.tool; then
              PUBLIC_OK=1
              break
            fi
            echo "Public endpoint not ready yet; retrying DNS/tunnel..."
            sleep 2
          done
          [ "$PUBLIC_OK" -eq 1 ]

          echo "[4/4] public noVNC HTTP"
          for _ in $(seq 1 15); do
            curl -4 --connect-timeout 5 --max-time 15 -fsSI "$TERMINAL_URL/novnc/vnc.html" && break
            sleep 2
          done
          curl -4 --connect-timeout 5 --max-time 15 -fsSI "$TERMINAL_URL/novnc/vnc.html"
          echo "Diagnostics passed."

      - name: Keep terminal and desktop alive
        shell: bash
        run: |
          set -euo pipefail
          echo "Terminal: $TERMINAL_PAGES_URL"
          echo "Desktop: $NOVNC_URL"
          echo "Runner services remain available until cancellation/timeout."
          while true; do
            sleep "${{ inputs.idle_seconds || '5' }}"
          done
